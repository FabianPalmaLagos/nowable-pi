# Nowable Pi chains

Reusable chains for Nowable ADW workflows.

## Chains

- `nowable.plan` — scout then produce a closed implementation plan.
- `nowable.adw` — plan, worker, verifier.
- `nowable.openspec-adw` — spec-aware plan, worker, verifier.
- `nowable.verify` — independent verification only.
